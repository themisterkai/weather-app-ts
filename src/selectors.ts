// HTML Selectors
export const mainSelector = document.getElementById('main');
export const geolocationLink = document.getElementById('geolink');
export const weatherPlaceholder = document.querySelector('.weather');
export const errorPlaceholder = document.querySelector('.error');
export const searchBar = document.getElementById(
  'search-bar'
) as HTMLInputElement;
export const forecastPlaceholder = document.querySelector('.forecast');
export const descriptionPlaceholder = document.getElementById('description');
export const fahrenheitController =
  document.getElementById('control-fahrenheit');
export const celciusController = document.getElementById('control-celcius');
export const iconLink = document.getElementById('icon') as HTMLImageElement;
